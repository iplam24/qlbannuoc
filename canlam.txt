- Làm thêm màn hình cho user sửa thông tin cá nhân gồm họ tên, địa chỉ, số điện thoại, đổi mật khẩu, làm màn hình admin xử lý thống kê sản phẩm theo ngày, tháng, năm( chức năng chọn thống kê), màn hình báo cáo trên trang admin,
trước mắt vậy đã để tao làm be cho


api thống kê,
router.get('/thongke/ngay/:date', getRevenueByDateAPI);
router.get('/thongke/thang/:year/:month', getRevenueByMonthAPI);
router.get('/thongke/nam/:year', getRevenueByYearAPI);
dùng 3 api này để xử lý
